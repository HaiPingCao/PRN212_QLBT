<UserControl x:Class="QLBT.Client.Teacher.Views.GradingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Bài tập:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Name="cb_Assignment" Width="320" DisplayMemberPath="Title" SelectionChanged="cb_Assignment_SelectionChanged" />
            <Button Name="btn_Refresh" Content="Tải lại" Padding="10,4" Margin="12,0,0,0" Click="btn_Refresh_Click" />
            <Button Name="btn_OpenSubmission" Content="Mở file bài nộp" Padding="10,4" Margin="8,0,0,0" Click="btn_OpenSubmission_Click" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox Name="tb_Search" Width="260" TextChanged="tb_Search_TextChanged" />
        </StackPanel>

        <DataGrid Grid.Row="2" Name="dg_Submissions" AutoGenerateColumns="False"
                  SelectionMode="Single" SelectionUnit="FullRow" CellEditEnding="dg_Submissions_CellEditEnding">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MSSV" Binding="{Binding StudentId}" Width="100" IsReadOnly="True" />
                <DataGridTextColumn Header="Họ tên" Binding="{Binding StudentName}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="File nộp" Binding="{Binding FileName}" Width="180" IsReadOnly="True" />
                <DataGridTextColumn Header="Số lần nộp" Binding="{Binding SubmitCount}" Width="90" IsReadOnly="True" />
                <DataGridTextColumn Header="Ngày nộp" Binding="{Binding SubmittedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="Điểm" Binding="{Binding Grade}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
