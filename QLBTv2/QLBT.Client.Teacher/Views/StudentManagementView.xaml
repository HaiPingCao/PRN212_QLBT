<UserControl x:Class="QLBT.Client.Teacher.Views.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- Danh sách sinh viên -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Danh sách sinh viên" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" />
                <Button Name="btn_New" Content="+ Thêm sinh viên mới" Padding="10,4" Margin="20,0,0,0" Click="btn_New_Click" />
                <Button Name="btn_Refresh" Content="Tải lại" Padding="10,4" Margin="8,0,0,0" Click="btn_Refresh_Click" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Name="tb_Search" Width="260" TextChanged="tb_Search_TextChanged" />
            </StackPanel>

            <DataGrid Grid.Row="2" Name="dg_Students" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                      SelectionChanged="dg_Students_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MSSV" Binding="{Binding Mssv}" Width="90" />
                    <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="*" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="220" />
                    <DataGridTextColumn Header="Số lớp đang học" Binding="{Binding SoLopDangHoc}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Form Thêm / Sửa -->
        <Border Grid.Column="1" BorderBrush="#CCCCCC" BorderThickness="1" Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock Name="tb_FormTitle" Text="Thêm sinh viên mới" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

                    <TextBlock Text="MSSV:" Margin="0,0,0,2" />
                    <TextBox Name="tb_Mssv" Margin="0,0,0,10" />

                    <TextBlock Text="Họ tên:" Margin="0,0,0,2" />
                    <TextBox Name="tb_HoTen" Margin="0,0,0,10" />

                    <TextBlock Text="Email:" Margin="0,0,0,2" />
                    <TextBox Name="tb_Email" Margin="0,0,0,10" />

                    <TextBlock Name="tb_MatKhauLabel" Text="Mật khẩu:" Margin="0,0,0,2" />
                    <PasswordBox Name="pb_MatKhau" Margin="0,0,0,2" />
                    <TextBlock Name="tb_MatKhauHint" Text="Để trống khi sửa nếu không muốn đổi mật khẩu."
                               Foreground="Gray" FontSize="11" Margin="0,0,0,10" Visibility="Collapsed" />

                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Name="btn_Save" Content="Lưu" Padding="14,6" Click="btn_Save_Click" />
                        <Button Name="btn_Cancel" Content="Hủy" Padding="14,6" Margin="8,0,0,0" Click="btn_Cancel_Click" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
