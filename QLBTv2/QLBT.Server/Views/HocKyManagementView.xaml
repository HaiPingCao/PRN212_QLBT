<UserControl x:Class="QLBT.Server.Views.HocKyManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Danh sách học kỳ" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" />
                <Button Name="btn_New" Content="+ Thêm học kỳ mới" Padding="10,4" Margin="20,0,0,0" Click="btn_New_Click" />
                <Button Name="btn_Refresh" Content="Tải lại" Padding="10,4" Margin="8,0,0,0" Click="btn_Refresh_Click" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" Margin="0,0,6,0" />
                <TextBox Name="tb_Search" Width="260" TextChanged="tb_Search_TextChanged" />
            </StackPanel>

            <DataGrid Grid.Row="2" Name="dg_HocKy" AutoGenerateColumns="False"
                      IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                      SelectionChanged="dg_HocKy_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Tên học kỳ" Binding="{Binding TenHocKy}" Width="*" />
                    <DataGridTextColumn Header="Ngày bắt đầu" Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" Width="110" />
                    <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding NgayKetThuc, StringFormat=dd/MM/yyyy}" Width="110" />
                    <DataGridTextColumn Header="Số lớp" Binding="{Binding SoLop}" Width="70" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Column="1" BorderBrush="#CCCCCC" BorderThickness="1" Padding="12">
            <StackPanel>
                <TextBlock Name="tb_FormTitle" Text="Thêm học kỳ mới" FontWeight="Bold" FontSize="14" Margin="0,0,0,12" />

                <TextBlock Text="Tên học kỳ:" Margin="0,0,0,2" />
                <TextBox Name="tb_TenHocKy" Margin="0,0,0,10" />

                <TextBlock Text="Ngày bắt đầu:" Margin="0,0,0,2" />
                <DatePicker Name="dp_BatDau" Margin="0,0,0,10" />

                <TextBlock Text="Ngày kết thúc:" Margin="0,0,0,2" />
                <DatePicker Name="dp_KetThuc" Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Name="btn_Save" Content="Lưu" Padding="14,6" Click="btn_Save_Click" />
                    <Button Name="btn_Cancel" Content="Hủy" Padding="14,6" Margin="8,0,0,0" Click="btn_Cancel_Click" />
                </StackPanel>

                <Separator Margin="0,16" />

                <Button Name="btn_Delete" Content="Xóa học kỳ này" Padding="10,6"
                        Background="#FFE5E5" Click="btn_Delete_Click" Visibility="Collapsed" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
